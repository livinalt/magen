export async function submitToGenLayer(data: any) {
  // MOCK for hackathon (replace with real SDK if available)

  return {
    txHash: "0x" + Math.random().toString(16).slice(2),
    status: "submitted",
  };
}